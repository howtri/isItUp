Checks the status of a website using TCP socket programming. 
Contains a python script to run from CLI and another to run as a Flask application.
Communicates with a sqlite db to store recently checked domain/IPs and list their statuses.

Future:
For CLI robust argparsing
Allow users to set times to recheck sites and notify of changed statuses
error codes for routes
logging
